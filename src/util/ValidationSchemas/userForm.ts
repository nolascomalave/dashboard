import * as userForm from './entityForm';

export const MAX_FILE_SIZE = userForm.MAX_FILE_SIZE;
export const ACCEPTED_IMAGE_TYPES = userForm.ACCEPTED_IMAGE_TYPES;
export const genders = userForm.genders;
export const userSchema = userForm.entitySchema;